// This is an object that defines different status codes, including loading, idle, and error.
const StatusCode = {
   LOADING: 'loading',
   IDLE: 'idle',
   ERROR: 'error'
}

export default StatusCode;
